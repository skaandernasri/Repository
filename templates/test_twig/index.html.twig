{% extends 'base.html.twig' %}

{% block title %}Hello TestTwigController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <img src="{{asset('hello.png')}}">
  {# {{controller_name}} #} 
   {{testRender}}
   {% set obj = {
    'fname':'Jane',
    'lname':"Doe"
   } %}

   {% set tab = [{'id':1,'name':'jane','email':'jane@example.com'},{'id':1,'name':'jane','email':'jane@example.com'}]%}
   {{obj['fname']}}
   <table>
    <tr>
        <th>ID</th>
        <th>Name</th>
        <th>Email</th>
    </tr>
    {% for p in tabP %}
    {% if p.id == 1 %}
        <tr>
            <td>{{p.id}}</td>
            <td>{{p.name|upper}}</td>
            <td>{{p.email}}</td>
        </tr>
    {% endif %}
    {% endfor %}

   </table>
  Nombre de personnes :  {{tabP|length}}
  {% if 5 not in [1,2,3,5]%}
  5 non présent
  {% else %}
  5 est présent
  {%endif%}

  {%if tabP is empty%}
  <p>Pas d'elements </p>
  {%endif%}

  {% set parametre = "test test" %}
  <a href="{{path('app_teacher')}}">Got to teacher </a>
  <a href="{{path('show_teacher',{'name':parametre})}}">Got to teacher 2 </a>

</div>
{% endblock %}
